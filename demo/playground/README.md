# Bittu's RPS engine — broken

This is the rock-paper-scissors logic Bittu uses to play judges through the
camera. The test suite is red. Fix `game.py` so `python3 run_tests.py` goes
green. Do not change `test_game.py` — it defines the correct behavior.
